# AiNotetaker — runtime dependencies
# Install with:  pip install -r requirements.txt
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
httpx>=0.27
python-multipart>=0.0.9

# Audio processing (recordings, chunking, noise removal)
imageio-ffmpeg>=0.5      # bundles an ffmpeg binary — no system install needed
soundfile>=0.12
numpy>=1.24
scipy>=1.10
noisereduce>=3.0         # lightweight default noise removal

# Optional, much better noise removal (DeepFilterNet, needs PyTorch):
#   pip install -r requirements-denoise.txt
